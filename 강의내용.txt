https://stepanowon.slack.com/

claude code : 아침 8시에 hello
gemini : 교육장와서 hello
opencode 는 안해도됨

claude --dangerously-skip-permissions -r

와이어프레임 만들때 ASCII 로 작성할 것 (토큰조금씀)
-> 이후 만들어진 ASCII 를 다음과 같이 명령어 날림 (WF-01 화면을 SVG로 작성해 (이미지로만들어줌))
-> 캡쳐한 이미지로 아래와 같이 캡쳐한 이미지로 스타일을 만들 수 있음
"[Image #1] 스크린 캡쳐한 이미지를 참조하여 프론트엔드 앱의 스타일 가이드를 작성해줘. docs/10-style-guide.md 로 저장해줘."
윈도우캡쳐 (윈도우키+shift+s), claude에 붙여넣기 alt + v

const express = require("express");
const { createMockMiddleware } = require("openapi-mock-express-middleware");
const swaggerUi = require("swagger-ui-express");
const swaggerDoc = require("../swagger/swagger.json"); // 또는 .yaml 파싱
const app = express();
app.use("/api", createMockMiddleware({ spec: "../swagger/swagger.json" })); // 목 서버
app.use("/docs", swaggerUi.serve, swaggerUi.setup(swaggerDoc)); // Swagger UI
app.listen(3000);

http://localhost:3000/docs/

강의내용업데이트

supabase myproject DB pw : O9fhZIu0fnI9AJ7C
DB_CONNECTION_STRING : postgresql://postgres.kwjtbuujhphetpiopfxe:O9fhZIu0fnI9AJ7C@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres

서버실행 (개발서버, backend, front 모두)
npm run dev 

backend 안에 .env 파일은 절대 수정하지 말것. env example 안에있는 파일을 수정할 것
env 파일은 자동으로 변경됨? 확인 필요

jhkwon-todolist-fe.vercel.app
jhkwon-todolist-be.vercel.app

FA01IS